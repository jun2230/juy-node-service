<%# const name = require("ejs/ejs").name; #%>

const verifyCreate<%= name %> = async (ctx, next) => {

    await next()

}

const verifyQuery<%= name %> = async (ctx, next) => {

    await next()

}

const verifyList<%= name %> = async (ctx, next) => {

    await next()

}

const verifyUpdate<%= name %> = async (ctx, next) => {

    await next()

}

const verifyDelete<%= name %> = async (ctx, next) => {

    await next()

}



module.exports = {
    verifyCreate<%= name %>,
    verifyQuery<%= name %>,
    verifyList<%= name %>,
    verifyUpdate<%= name %>,
    verifyDelete<%= name %>
}
